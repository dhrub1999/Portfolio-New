'use client';
import { useState, useEffect } from 'react';
import { motion } from 'framer-motion';

const GlitchText = ({ mainText }) => {
  return <h1>{mainText}</h1>;
};

export default GlitchText;
